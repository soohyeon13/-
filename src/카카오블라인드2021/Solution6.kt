package 카카오블라인드2021

import java.util.*
import kotlin.collections.ArrayList

private val dx = intArrayOf(-1, 0, 1, 0)
private val dy = intArrayOf(0, 1, 0, -1)
private val cardArr = ArrayList<IntArray>()
private val a = intArrayOf(1,2,3,4,5,6)
class Solution6 {
    fun solution(board: Array<IntArray>, r: Int, c: Int): Int {
        var answer: Int = 0
        return answer
    }
}