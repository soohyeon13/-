package 백준

fun main() {
    val br = System.`in`.bufferedReader()
    val bw = System.out.bufferedWriter()
    val shortcut = br.readLine().split(" ")
    var totalCnt = 0
    shortcut.forEach { totalCnt+=it.toInt() }
    for (i in 0 until  totalCnt ) {

    }
}